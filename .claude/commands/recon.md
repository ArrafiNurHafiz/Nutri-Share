---
description: Full recon pipeline — subdomain enum, live hosts, URL crawl, nuclei sweep. Usage: /recon target.com
---

# /recon

Full recon pipeline using tools/recon_engine.sh.

```bash
bash tools/recon_engine.sh target.com
bash tools/recon_engine.sh target.com --quick
```

Output: recon/<target>/
