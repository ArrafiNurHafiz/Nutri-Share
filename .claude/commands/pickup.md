---
description: Reload previous hunt session context. Usage: /pickup <target>
---

# /pickup

Reload previous hunt session from hunt memory.

Loads the last SESSION.md, active leads, findings, and scope context for a target so you can continue where you left off.

Uses hunt-memory/ to store session state between Claude Code sessions.
